// Deprecated — was used by the previous Hero CTA which has been removed.
// Safe to delete from disk. Kept as a stub so its absence is intentional.
// If a magnetic interaction is ever needed elsewhere, restore from git history.

export {};
