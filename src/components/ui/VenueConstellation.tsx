// Deprecated — replaced by DistrictMap.tsx, which is on-brand for TapIn:
// a real cartographic backdrop (streets + venues + grain texture) instead of
// the radar-feeling pulsing constellation.
// Safe to delete from disk; left as a stub so its absence is intentional.

export {};
